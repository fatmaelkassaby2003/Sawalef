# 📦 Postman Collection - نظام الشات

## ✅ تم إنشاء الـ Collection!

الملف الجاهز: **`Sawalef_Chat_API.postman_collection.json`**

---

## 📥 **كيفية الاستيراد في Postman:**

### الطريقة 1: السحب والإفلات
1. افتح **Postman**
2. اسحب ملف `Sawalef_Chat_API.postman_collection.json`
3. أفلته في نافذة Postman
4. ✅ تم!

### الطريقة 2: Import Button
1. افتح **Postman**
2. اضغط **Import** (أعلى اليسار)
3. اختر **Upload Files**
4. اختر ملف `Sawalef_Chat_API.postman_collection.json`
5. اضغط **Import**
6. ✅ تم!

---

## 🎯 **ما يحتويه الـ Collection:**

### 📁 **Chat** (4 endpoints)
1. ✅ **Start Conversation** - بدء محادثة مع مستخدم
2. ✅ **Get All Conversations** - جلب قائمة المحادثات
3. ✅ **Get Messages** - جلب رسائل محادثة معينة
4. ✅ **Send Message** - إرسال رسالة (مع Pusher broadcast)

### 📁 **Auth** (3 endpoints)
1. Register - تسجيل مستخدم جديد
2. Login (Send OTP) - إرسال OTP
3. Verify OTP - التحقق من OTP (يحفظ الـ Token تلقائياً)

### 📁 **Wallet** (3 endpoints)
1. Get Balance - جلب الرصيد
2. Get Payment Methods - جلب طرق الدفع
3. Deposit - شحن المحفظة

---

## ⚙️ **المتغيرات (Variables):**

الـ Collection يحتوي على متغيرين:

1. **`base_url`** = `http://127.0.0.1:8000`
   - ✏️ عدّلها إذا كان الـ server على رابط مختلف

2. **`token`** = (فارغ في البداية)
   - 🔑 يتم ملؤه تلقائياً بعد Verify OTP

---

## 🚀 **كيفية الاستخدام:**

### 1️⃣ **تسجيل الدخول أولاً:**
```
Auth → Login (Send OTP)
Auth → Verify OTP
```
عندما تنجح عملية Verify، الـ Token يُحفظ تلقائياً!

### 2️⃣ **ابدأ محادثة:**
```
Chat → Start Conversation
```
غيّر `user_id` إلى المستخدم الذي تريد المحادثة معه.

### 3️⃣ **أرسل رسالة:**
```
Chat → Send Message
```
غيّر رقم الـ conversation في الـ URL (مثلاً: `/conversations/1/messages`)

### 4️⃣ **شاهد الرسائل:**
```
Chat → Get Messages
```

---

## 🔴 **اختبار Pusher (Real-time):**

### الخطوات:
1. افتح **Pusher Dashboard**: https://dashboard.pusher.com
2. اختر الـ App الخاص بك
3. انتقل لـ **"Debug Console"**
4. ارجع لـ Postman وأرسل رسالة عبر `Send Message`
5. **شاهد**: Event `message.sent` يظهر فوراً في Pusher! 🎉

---

## 📝 **نصائح:**

### تغيير الـ Base URL:
إذا كان الـ server على عنوان مختلف:
1. اضغط على اسم الـ Collection
2. اذهب لـ **Variables**
3. غيّر `base_url` القيمة الحالية إلى القيمة الجديدة
4. احفظ

### نسخ الـ Token يدوياً:
إذا لم يُحفظ الـ Token تلقائياً:
1. انسخ الـ Token من response الـ Verify
2. اضغط على الـ Collection → **Variables**
3. ضع الـ Token في متغير `token`
4. احفظ

---

## ✅ **جاهز للاستخدام!**

استورد الـ Collection وابدأ الاختبار فوراً! 🚀💬
